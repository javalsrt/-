# 课程笔记助手（Course Note Assistant）

## 项目简介

课程笔记助手是一套面向高校师生的课程资料管理与 AI 辅助学习系统。它以课程表为入口，把拍照、手写笔记、课件导入等材料自动归集到每一次课段，并通过大语言模型完成课段总结与基于知识库的智能问答。

系统包含三个子项目：

| 子项目 | 技术栈 | 目标用户 | 目录 |
|--------|--------|----------|------|
| 后端服务 | Spring Boot 3 + MyBatis-Plus + MySQL | 小程序 / 教师端 | `course-note-backend/` |
| 学生小程序 | 微信小程序原生框架 | 学生 | `course-note-miniapp/` |
| 教师管理端 | Vue 3 + Vite + TDesign | 教师 | `course-note-teacher/` |

## 核心功能

### 学生端（小程序）

- **微信登录**：调用 `wx.login` 获取 code，后端换取 openid 后签发 JWT Token。
- **本周课表**：按班级精确匹配课程，按星期分组展示。
- **上课 / 下课**：点击开始上课生成 `course_session` 记录，下课回填实际结束时间。
- **资料采集**：支持拍照、文字笔记、外部资料导入，统一存入 `material` 表并关联课段。
- **AI 课段总结**：将一节课的所有资料拼接成 prompt，调用 LLM 生成总结并写回课段。
- **知识库问答**：通过 WebSocket 流式对话，AI 优先使用课程知识库文档作答。

### 教师端（Web）

- **账号密码登录**：演示阶段使用硬编码教师密码表，登录后同样使用 JWT。
- **仪表盘**：展示当前教师所教课程的学生数、资料数、知识库文档数及近 30 天趋势。
- **课表管理**：添加、编辑、删除课程，支持导入导出。
- **知识库管理**：上传文档到指定课程，查看索引状态并删除文档。
- **数据统计**：按课程、班级、学生维度展示图表。

## 技术架构

```
┌──────────────────┐    HTTP/JSON     ┌────────────────────────┐    MyBatis    ┌─────────┐
│  微信小程序       │ ───────────────► │  Spring Boot 后端       │ ────────────► │  MySQL  │
│  （学生端）        │                  │  - Controller          │               └─────────┘
└──────────────────┘                  │  - Service             │
┌──────────────────┐    HTTP/JSON     │  - Mapper              │    WebSocket
│  Vue 教师管理端   │ ───────────────► │  - JWT 鉴权            │ ◄──────────┐
└──────────────────┘                  │  - WebSocket           │            │
                                      └────────────────────────┘            │
                                                ▲                           │
                                                │  调用 LLM / OCR / Embedding│
                                                └───────────────────────────┘
```

后端关键依赖：

- Spring Boot 3.2（JDK 17）
- MyBatis-Plus 3.5.5
- MySQL 8
- JJWT 0.12.3
- Spring WebSocket
- Spring WebFlux（调用 LLM）
- Hutool、Jsoup

## 目录结构

```
course-note-backend/
├── src/main/java/com/coursenote/
│   ├── common/         # Result、全局异常、自动填充字段
│   ├── config/         # JWT、跨域、WebSocket、MyBatis-Plus
│   ├── controller/     # RESTful 接口
│   ├── dto/            # 请求参数封装
│   ├── entity/         # 数据库实体
│   ├── mapper/         # 数据访问层
│   ├── service/        # 业务逻辑层
│   │   └── impl/
│   └── ws/             # AI 对话 WebSocket 处理器
├── src/main/resources/
│   └── application.yml # 主配置
├── sql/                # 数据库脚本
└── pom.xml

course-note-miniapp/
├── pages/              # 小程序页面
│   ├── index/          # 课表首页
│   ├── course/         # 课程管理
│   ├── session/        # 课段详情
│   ├── photo/          # 拍照上传
│   ├── note/           # 文字笔记
│   ├── knowledge/      # 知识库与 AI 问答
│   ├── profile/        # 个人中心
│   └── material-detail/# 资料详情
├── utils/              # API 封装与工具函数
└── app.json

course-note-teacher/
├── src/
│   ├── views/          # 页面组件
│   ├── components/     # 公共组件
│   ├── layouts/        # 布局组件
│   ├── router/         # 路由配置
│   ├── stores/         # Pinia 状态
│   ├── utils/          # 请求封装
│   └── styles/         # 全局样式
├── package.json
└── vite.config.js
```

## 数据库设计

核心表说明：

| 表名 | 说明 |
|------|------|
| `user` | 用户表，学生按 `grade` 字段匹配课程 |
| `course` | 课程表，`description` 字段用于存放精确班级名 |
| `course_session` | 课段表，记录一次实际上课的开始/结束时间 |
| `material` | 资料表，统一存储照片、笔记、外部导入资料 |
| `knowledge_base` | 知识库表 |
| `knowledge_document` | 知识库文档表，含索引状态 |
| `ai_chat_record` | AI 对话记录表 |

## 关键接口

### 学生端

| 接口 | 说明 |
|------|------|
| `POST /api/user/login` | 微信登录 |
| `GET /api/courses/schedule` | 本周课表 |
| `POST /api/sessions/start/{courseId}` | 开始上课 |
| `POST /api/sessions/end/{sessionId}` | 下课 |
| `POST /api/materials/photo` | 上传照片 |
| `POST /api/sessions/{id}/summarize` | AI 总结课段 |
| `WS /api/ws/ai/chat` | 流式 AI 问答 |

### 教师端

| 接口 | 说明 |
|------|------|
| `POST /api/teacher/login` | 教师登录 |
| `GET /api/teacher/dashboard` | 仪表盘数据 |
| `GET /api/teacher/courses` | 教师课程列表 |
| `POST /api/teacher/courses` | 添加课程 |
| `POST /api/teacher/knowledge/upload` | 上传知识库文档 |
| `GET /api/teacher/statistics` | 数据统计 |

## 快速启动

### 1. 初始化数据库

```bash
cd course-note-backend
mysql -u root -p < sql/init.sql
mysql -u root -p < sql/test_data.sql
```

### 2. 启动后端

编辑 `course-note-backend/src/main/resources/application.yml`，填入：

- MySQL 连接信息
- 微信小程序 `app-id` / `app-secret`
- LLM API Key（DeepSeek / 其他 OpenAI 兼容接口）
- 文件上传目录

然后执行：

```bash
cd course-note-backend
mvn spring-boot:run
```

后端默认运行在 `http://localhost:8080/api`。

### 3. 启动教师管理端

```bash
cd course-note-teacher
npm install
npm run dev
```

默认地址为 `http://localhost:3000`，Vite 代理已配置到 `localhost:8080`。

### 4. 运行小程序

使用微信开发者工具打开 `course-note-miniapp` 目录：

- 修改 `utils/api.js` 中的 `BASE_URL` 为后端地址。
- 在 `app.json` 中配置小程序 AppID。

## 重要约定

- `course.description` 存放精确班级名（如 `计科2201`），学生 `grade` 必须与之完全匹配才能看到课程。
- 教师端所有查询都基于 JWT 中的教师 ID，只能看到自己所教课程的数据。
- 知识库问答按“课程名 + 班级”聚合所有相关 `course_id`，避免漏掉不同班级下教师上传的文档。
- AI 问答优先走 WebSocket 流式通道，连接失败时自动降级到 `POST /api/ai/chat`。

## 已知注意事项

- `start_backend.bat` 中写死了旧项目路径，使用前请根据实际路径修改。
- 腾讯云 OCR / ASR 为可选能力，未配置时不影响核心流程。
- 教师密码目前硬编码在 `TeacherController` 中，仅用于演示。
